! Let command successful test

let
   proc p() ~ let
                 var x: Integer 
	       in
                   getint(var x);
                   putint(x)
               end;

   var x: Char;
   var y: Integer
in
  get(var x);
  put(x);
  let
    var y: Integer 
  in
    getint(var y);
    putint(y) ! It uses the first declaration
  end
end
